# Normalize text files to LF in the repository, regardless of contributor OS.
* text=auto eol=lf

# Windows-native scripts must stay CRLF to run correctly.
*.ps1 text eol=crlf
*.bat text eol=crlf

# Binary assets: never touch line endings.
*.png binary
*.ico binary
*.exe binary
